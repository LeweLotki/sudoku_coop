"""FastAPI application entrypoint (placeholder).

TODO: Wire up the real application in a future change. The planned setup includes:
  - CORS configuration sourced from ``core.config`` (ALLOWED_ORIGINS)
  - The health router from ``api.health``
  - The WebSocket router/endpoint backed by ``websocket.connection_manager``

No functional behavior is implemented yet (scaffolding only).
"""

# TODO: from fastapi import FastAPI
# TODO: app = FastAPI(title=...)
# TODO: include health router and websocket route.
