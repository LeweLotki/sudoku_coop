services:
  app:
    build: backend/
    container_name: sudokupad-coop
    command: uv run uvicorn sudoku_coop_api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    restart: unless-stopped