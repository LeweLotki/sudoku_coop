// Highlight overlay rendering (placeholder).
//
// Future behavior (out of scope for the scaffolding change): draw a DOM overlay
// rectangle mapped from (row, column) on top of the grid using:
//   - position: absolute
//   - high z-index
//   - pointer-events: none
//   - transparent colored background
//   - border / glow
//   - fade after a few seconds; latest highlight may replace the previous one

export {};

// TODO: export function highlightCell(row: number, column: number): void { ... }
