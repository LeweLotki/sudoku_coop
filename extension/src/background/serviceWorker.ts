// Background service worker (placeholder).
//
// Future responsibilities (out of scope for the scaffolding change):
//   - coordinate the extension-level WebSocket connection/state if needed
//   - relay messages between the popup and the content script
//   - persist current mode / sessionId in extension storage if needed
//
// No functional behavior yet (scaffolding only).

export {};

// TODO: chrome.runtime.onMessage.addListener(...) etc.
